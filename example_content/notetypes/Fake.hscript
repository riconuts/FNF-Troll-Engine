// note that is a litle bit lighter and cannot be hit

function setupNote(note:Note){
    note.texture = 'FAKENOTE_assets';
    note.ignoreNote = true;
}

function judgeNote(note:Note, msDiff:Float){
    return Judgement.UNJUDGED; // can never be hit
}